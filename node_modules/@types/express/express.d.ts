// express.d.ts
import { IUser } from "../../../backend/src/model/userModel";

declare module "express-serve-static-core" {
  interface Request {
    user?: IUser;  // Define 'user' as an optional property
  }
}
